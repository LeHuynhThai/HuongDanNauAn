# Xcode build files and derived data
build/
DerivedData/

# Xcode user settings and workspace data
xcuserdata/
*.xcworkspace
*.xcuserdatad

# CocoaPods (Nếu bạn có dùng)
# Nếu bạn dùng Cocoapods, hãy bỏ comment dòng dưới đây. 
# Tuy nhiên, khuyến khích giữ Podfile.lock lại.
# Pods/ 

# Swift Package Manager (SPM)
.swiftpm/

# macOS system files and configuration
.DS_Store

# User-generated files (File không nên push lên server)
AppDatabase.sqlite3